# Application Configuration
spring.application.name=my-bakery
server.port=8080

# Admin Configuration
admin.username=admin@mybakery.com
admin.password=Admin@123


# Database Configuration - PostgreSQL (for production)
spring.datasource.url=jdbc:postgresql://turntable.proxy.rlwy.net:10575/railway
spring.datasource.username=postgres
spring.datasource.password=wmhAlVoFtoFGWHxVAbYboLlaxbFzCioL
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT Configuration
jwt.secret=ProdSecretKey123456789012345678901234567890AbCdEfGhIjKlMnOpQrStUvWxYz
jwt.expiration=86400000
jwt.refresh-expiration=604800000

# File Upload Configuration
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB
file.upload-dir=/opt/my-bakery/uploads

# Cloudinary Configuration
cloudinary.cloud-name=do0x8z2ju
cloudinary.api-key=244796363567388
cloudinary.api-secret=o7-FbqZE22JAKv4tzYieOITQIZw

# Logging Configuration
logging.level.root=WARN
logging.level.com.aurelius.tech.mybakery=INFO
logging.level.org.springframework.web=WARN
logging.level.org.hibernate=ERROR
logging.file.name=/var/log/my-bakery/application.log
logging.file.max-size=10MB
logging.file.max-history=10

# Swagger/OpenAPI Configuration
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method
springdoc.api-docs.enabled=false
springdoc.swagger-ui.enabled=false

# Actuator Configuration
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=never

# Server Configuration
server.compression.enabled=true
server.http2.enabled=true
server.tomcat.max-threads=200
server.tomcat.min-spare-threads=20

# Cache Configuration
spring.cache.type=caffeine
spring.cache.caffeine.spec=maximumSize=500,expireAfterAccess=600s

# Connection Pool Configuration
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.connection-timeout=30000

# SSL Configuration (if applicable)
# server.ssl.key-store=classpath:keystore.p12
# server.ssl.key-store-password=your-keystore-password
# server.ssl.key-store-type=PKCS12
# server.ssl.key-alias=your-key-alias
# server.ssl.enabled=true

# CORS Configuration
cors.allowed-origins=http://localhost:3000,http://localhost:3001,https://*.railway.app,https://*.vercel.app,https://*.netlify.app
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS,PATCH
cors.allowed-headers=*
cors.exposed-headers=x-auth-token,Authorization,Content-Type,Accept,Origin,Access-Control-Request-Method,Access-Control-Request-Headers
cors.allow-credentials=true
cors.max-age=3600